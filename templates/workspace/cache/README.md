# cache/ — dựng lại được

Cache của công cụ (không phải cache weights của Hugging Face — cái đó nằm ở `HF_HOME`). Không vào
`export`, không vào `backup`. Xoá thoải mái; lần chạy sau tự dựng lại, chỉ chậm hơn một chút.
